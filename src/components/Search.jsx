const Search = ({searchText, setSearchText}) => {    return (        <div className="search">            <div>                <img src="../../public/search.svg" alt="Search"/>                <input                    type="text"                    placeholder="Search the thousand of Movies"                    value={searchText}                    onChange={(e) => setSearchText(e.target.value)}                />            </div>        </div>    )}export default Search;